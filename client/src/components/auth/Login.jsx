import React from 'react';

const Login = () => {
	return <div>Login</div>;
};
//Login will be around the same thing as the register, the pathing, consider reusing login as a register component aswell
export default Login;
